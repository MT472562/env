# ~/.ssh/config に配置される（秘密鍵はリポジトリに含めない）
#
# GitHub の認証は gh（HTTPS）に統一。
# ここは LAN / クラウド等「サーバ接続用」の定義。

# --- LAN ---
Match host 10.10.0.*
  User server
  IdentityFile ~/.ssh/lan.key
  IdentitiesOnly yes

# --- クラウド / 個人ホスト ---
Match host *.maruchan.dev,*.v4v6.net,*.sh3.uk
  IdentityFile ~/.ssh/oci.key
  IdentityFile ~/.ssh/gcp.key
  IdentitiesOnly yes
